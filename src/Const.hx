class Const {
  public static var FIXED_FRAMES(default, never) = 30;
  public static var FIXED_TIME_STEP(default, never) = 1./30.;
  public static var MAX_CYCLES_PER_FRAME(default, never) = 8;

  // public static var RENDER_HEIGHT(default, never) = 640;
  // public static var RENDER_WIDTH(default, never) = 360;
}