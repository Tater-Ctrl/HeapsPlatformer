{
	"version": "0.2.0",
	"configurations": [

        {
            "name": "HashLink",
            "type": "hl",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "release",
            "windows": {
                // Make windows compatible launch settings
            }
        }
	]
}